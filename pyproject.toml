[project]
name = "django-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "gunicorn>=23.0.0",
    "python-dotenv>=1.1.1",
    "ruff>=0.14.1",
]
